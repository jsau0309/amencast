# WebSocket Server Environment Variables

# Server Configuration (Optional - defaults shown)
PORT=3000

# Redis Configuration (Optional - defaults shown)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_TLS_ENABLED=false

# Queue Names (Optional - defaults shown)
REDIS_INGESTION_QUEUE_NAME=ingestion_jobs_queue
REDIS_RESULTS_QUEUE_NAME=translation_results_queue